# PhoneFold Phase 0 (Model Forge) toolchain.
# Python 3.12 (Homebrew) — torch publishes no 3.14 wheels as of 2026-08.
#
# ESMFold is taken from HuggingFace transformers (EsmForProteinFolding) rather than
# fair-esm[esmfold]: the latter pulls in openfold, whose fused CUDA kernels do not build
# on Apple Silicon. The transformers implementation is pure PyTorch and runs on CPU/MPS,
# which is what the Core ML export path needs anyway.

torch==2.9.1
transformers==4.57.1
accelerate==1.10.1
coremltools==9.0
numpy==2.1.3
scipy==1.15.3
biotite==1.6.0
